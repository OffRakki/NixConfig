# Audit the NixConfig flake

Run the nix-auditor subagent on `~/Projects/NixConfig` and report:
- Dead code or unused inputs
- Redundancy between modules
- Package deduplication opportunities
- Improvement suggestions with priority

Use the nix-auditor skill and agent.

# Commit current work

Run `jj status` and `jj log -r '@-'` to review current state, then write a
descriptive commit message summarizing the changes.
Use `jj describe -m "..."` then `jj new` to keep `@` fresh.
